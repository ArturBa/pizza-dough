import { InputAfterDirective } from './input-after.directive';

describe('InputAfterDirective', () => {
  it('should create an instance', () => {
    const directive = new InputAfterDirective();
    expect(directive).toBeTruthy();
  });
});
